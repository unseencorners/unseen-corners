{
  "name": "unseen-corners",
  "version": "1.0.0",
  "description": "Unseen Corners tourism website with authentication",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "mongodb": "^5.0.0",
    "dotenv": "^16.0.0"
  },
  "engines": {
    "node": ">=14"
  }
}